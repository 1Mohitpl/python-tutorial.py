set = {10, 56, 89, 90, 'mohit', True}
print(set)
# sets are unordered, 
#in sets duplicacy is not allowd
# in sets, indexing access is not allowed
# in sets, we cannot modifiy single item, instead we can add one iteam at a time by the "add" method
# in sets, we cannnot add list in a sets beacause list are immutable.
set.add(23)
print(set)